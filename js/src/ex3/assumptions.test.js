import fc from 'fast-check';

const div = (a, b) => a / b;

test('dividing a number by 1 returns itself', () => {
    fail('not implemented')
});

test('dividing any non zero number by itself returns 1', () => {
    fail('not implemented')
});

test('multiplying two numbers returns a negative number if one of them is negative', () => {
    fail('not implemented')
});