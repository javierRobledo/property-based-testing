import fc from 'fast-check';
import repository from './repository';

test('an entity can be fetched after saving it', () => {
    /*
    Ejemplos de entidad Person:
    - { id: 0, name: "Marta", age: 32 }
    - { id: 1, name: "Rodri", age: 21 }
     */
});